#include <iostream>
using namespace std;

int main()
{
 cout<<"Iostream:- \n \t The header file 'iostream' should be included at the beginning of all programs that use input/ outpur statements"<<endl;
 cout<<"Namespace:- \n \t Namespace defines a scope for the identifiers that are used in a program. For using the identifiers defined in the namespace scope we must include the using directive."<<endl;
 cout<<"Return Type:- \n \t In C++, main() returns an intergers type value to the operating system. Therefore, every main() in C++ should end with a return(0) statement; otherwise a warning or an error might occur. Since main() returns an integer type caur, return type for main() is explicitly specified as int. Note that the default return type for all functions in C++ in int."<<endl;
 return 0;
}
